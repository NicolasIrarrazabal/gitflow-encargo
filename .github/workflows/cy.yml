name: Github actions basico

on:
  push:
    branches: [develop, main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v3

      - name: Mostrar mensaje
        run: echo "Pipeline ejecutado correctamente"